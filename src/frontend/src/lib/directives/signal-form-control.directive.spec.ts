import { SignalFormControlDirective } from './signal-form-control.directive';

describe('SignalFormControlDirective', () => {
  it('should create an instance', () => {
    const directive = new SignalFormControlDirective();
    expect(directive).toBeTruthy();
  });
});
