mutation addBookDialogData_addBook(
  $name: String!
  $description: String!
  $startMonth: Int!
) {
  createBook(
    name: $name
    currency: "EUR"
    description: $description
    startMonth: $startMonth
  ) {
    id
  }
}
