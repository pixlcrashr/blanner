import { HierarchyPrefixPipe } from './hierarchy-prefix.pipe';

describe('HierarchyPrefixPipe', () => {
  it('create an instance', () => {
    const pipe = new HierarchyPrefixPipe();
    expect(pipe).toBeTruthy();
  });
});
